name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  # --- JOB 1: THE SAFETY CHECKS (CI) ---
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # Path updated to app/ folder
          pip install -r app/requirements-dev.txt

      - name: Check Formatting (Black)
        # Paths updated to app/ and tests/ folders
        run: black --check app/monitor.py tests/test_monitor.py

      - name: Run Linter (Flake8)
        # Paths updated to app/ and tests/ folders
        run: flake8 app/monitor.py tests/test_monitor.py

      - name: Run Unit Tests (Pytest)
        # Path updated to tests/ folder
        run: pytest tests/test_monitor.py

      - name: Terraform Security Scan (tfsec)
        uses: aquasecurity/tfsec-action@v1.0.0

  # --- JOB 2: THE DEPLOYMENT (CD) ---
  deploy:
    needs: test 
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push to ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: project-argus-monitor
          IMAGE_TAG: ${{ github.sha }} 
        run: |
          # We keep the build context as "." (the root) 
          # so Docker can see the Dockerfile and the app/ folder
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      - name: Update Lambda Function
        run: |
          aws lambda update-function-code \
            --function-name project-argus-monitor-function \
            --image-uri ${{ steps.login-ecr.outputs.registry }}/project-argus-monitor:${{ github.sha }}